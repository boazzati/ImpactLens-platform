Frontend Deployment Trigger
Timestamp: $(date)

This file forces Vercel to rebuild the frontend with the latest code.

Issue: Frontend still using old proxy route instead of direct Heroku connection.
Expected: Should use https://impactlens-platform-20d6698d163f.herokuapp.com/api/test-openai directly.

Build settings applied:
- Build Command: cd frontend && pnpm install && pnpm run build
- Output Directory: frontend/dist
- Install Command: cd frontend && pnpm install
